<Page x:Class="OTKApp.SignInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OTKApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SignInPage">


    <Grid Background="#99FFFFFF" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border 
            BorderThickness="2"
            BorderBrush="Black"
            CornerRadius="6">
            <StackPanel Margin="10">

                <TextBlock Text="Login" />
                <TextBox Margin="0 10" Name="tbLogin" />

                <TextBlock Text="Password" />
                <PasswordBox Margin="0 10" Name="tbPassword" />

                <Button Padding="4" Content="Login" Click="SignInButton_Click"/>

            </StackPanel>

        </Border>

    </Grid>
</Page>
